# Copie este arquivo para .env e ajuste os valores para o seu ambiente.
# O arquivo .env nunca deve ser commitado no repositório.

# ─── PostgreSQL ──────────────────────────────────────────────────────────────
POSTGRES_DB=auth_db
POSTGRES_USER=auth_user
POSTGRES_PASSWORD=auth_pass

# ─── auth-service ────────────────────────────────────────────────────────────
# String de conexão — deve apontar para o serviço "postgres" dentro da rede Docker
DATABASE_URL=postgres://auth_user:auth_pass@postgres:5432/auth_db?sslmode=disable

# Porta exposta no host
PORT=8001

# Chave mestra para o endpoint /admin/keys — troque em produção!
MASTER_KEY=admin-secreto-123
