apiVersion: v1
kind: Secret
metadata:
  name: evaluation-service-secrets
  namespace: togglemaster
type: Opaque
stringData:
  # Gerar após o auth-service estar Running:
  # POST http://auth-service:8001/admin/keys  (via port-forward)
  # Documentar o valor gerado em local seguro (não commitar o valor real)
  SERVICE_API_KEY: "PLACEHOLDER_SERVICE_API_KEY"
