apiVersion: v1
kind: ServiceAccount
metadata:
  name: evaluation-service
  namespace: togglemaster
  annotations:
    # Role IRSA ÚNICA compartilhada pelas apps do namespace (módulo iam-irsa, irsa.tf).
    # Também disponível no SSM: /irsa/role-eks-togglemaster-apps/role-arn
    # Trust policy: system:serviceaccount:togglemaster:* (qualquer SA do namespace)
    eks.amazonaws.com/role-arn: "arn:aws:iam::650687537445:role/role-eks-togglemaster-apps"
