apiVersion: v1
kind: ConfigMap
metadata:
  name: targeting-service-config
  namespace: togglemaster
data:
  # URL interna do auth-service — targeting-service valida a chave de API a cada request
  AUTH_SERVICE_URL: "http://auth-service:8001"
